# FlexSeedScheme Example

Example that demonstrates how to use the FlexSeedScheme package.

## Getting Started

This example shows how to use the FlexSeedScheme package by defining one and optionally up to six
key colors to seed generate used `ColorScheme` colors. You can also add chroma and tone
mapping configuration using `FlexTones`, by using pre-defined options or defining your own.
